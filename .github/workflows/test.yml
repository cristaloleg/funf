name: test
on: workflow_dispatch

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Do
        run: |
          echo "Slug variables"
          echo $${{ github.ref }}
          echo http://${{ github.ref }}.lel
