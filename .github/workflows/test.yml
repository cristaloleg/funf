name: test
on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Do
        run: |
          echo "Slug variables"
          echo $${{ github.ref }}
          echo http://${{ github.ref }}.lel
          echo http://${{ GITHUB_BRANCH##*/ }}.lel
